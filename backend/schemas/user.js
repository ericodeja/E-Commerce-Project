class User {
  constructor(id,fullname, email, password, role) {
    this.id = id
    this.fullname = fullname;
    this.email = email;
    this.password = password;
    this.role = role;
  }
}

export default User;
